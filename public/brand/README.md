# Branding

Placez votre logo dans ce dossier :

- `public/brand/logo.png`

Ce fichier sera utilisé :

- dans l’en-tête (composant `components/brand/Logo.tsx`) ;
- pour les icônes d’application (PWA) via la route `/icons/<size>` (fallback automatique si le fichier n’existe pas).
